import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default {
  H1({props}: any) {
    return (
      <Text>{props}</Text>
    );
  }
};

const styles = StyleSheet.create({

});
